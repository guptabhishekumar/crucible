# Python 3.12. On Windows, `pip install torch` already gives the CPU build;
# on Linux/macOS use the CPU index to keep the footprint small:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
stable-baselines3>=2.3
sb3-contrib>=2.3
gymnasium>=0.29
numpy>=1.26
pandas>=2.2
scipy>=1.11
matplotlib>=3.8
dukascopy-python>=4.0.1
pytest>=8.0
